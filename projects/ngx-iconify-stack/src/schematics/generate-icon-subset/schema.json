{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsNgxIconifyStackGenerateIconSubset",
  "title": "Generate Icon Subset",
  "description": "Scans your project for icon usage, builds an offline subset, and wires it into the provider",
  "properties": {
    "project": {
      "type": "string",
      "description": "Target project name (required in multiproject workspaces)",
      "x-prompt": "Which project should receive the icon subset?",
      "$default": {
        "$source": "projectName"
      }
    }
  },
  "required": ["project"]
}
