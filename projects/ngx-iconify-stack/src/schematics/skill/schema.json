{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsNgxIconifyStackSkill",
  "title": "Generate the ngx-iconify-stack AI agent skill",
  "description": "Regenerates the AI agent skill (.agents/skills/ngx-iconify-stack) in the target project",
  "properties": {
    "project": {
      "type": "string",
      "description": "Target project name (defaults to the first application project)",
      "x-prompt": "Which project should receive the ngx-iconify-stack skill?",
      "$default": {
        "$source": "projectName"
      }
    }
  },
  "required": []
}
