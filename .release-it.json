{
  "git": {
    "commitMessage": "chore(release): v${version}",
    "tagName": "v${version}",
    "requireCleanWorkingDir": true,
    "requireBranch": "main"
  },
  "npm": {
    "publish": false
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "angular",
      "infile": "CHANGELOG.md"
    }
  },
  "hooks": {
    "before:init": ["ng test ngx-iconify-stack --watch=false", "npm run build:lib"],
    "after:bump": [
      "node -e \"const fs=require('fs'); [ './package.json', './projects/ngx-iconify-stack/package.json' ].forEach(f => { const p=JSON.parse(fs.readFileSync(f, 'utf8')); p.version='${version}'; fs.writeFileSync(f, JSON.stringify(p, null, 2) + '\\n'); })\"",
      "npm run build:lib"
    ]
  }
}
